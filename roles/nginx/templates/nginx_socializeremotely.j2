
{% extends "strict_ssl_base.j2" %}

{% block index_location %}
{% endblock %}
{% block error_pages %}
{% endblock %}
{% block error_location %}
{% endblock %}
{% block robots_location %}
{% endblock %}

{% block custom_location %}

    location / {
        try_files $uri @proxy_to_app;
    }

    location @proxy_to_app {
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Host $http_host;
        proxy_read_timeout {{ express_timeout_seconds }};
        proxy_pass http://{{ service_express_host }}:{{ service_express_port }};
    }

{% endblock %}
